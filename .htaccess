RewriteEngine On

# Do not rewrite real files or directories.
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Send every application route to index.php.
RewriteRule ^ index.php [QSA,L]